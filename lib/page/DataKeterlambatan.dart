import 'package:flutter/material.dart';

class DataKeterlambatan extends StatelessWidget {
  const DataKeterlambatan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}